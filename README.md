# 🐱 Конкурс «Лучшая кошка района»

## Что реализовано

### ✅ Функционал
- **Загрузка фото** — drag & drop или клик
- **Голосование** — лайки с защитой от накрутки (один голос с устройства)
- **Рейтинг** — сортировка по голосам, топ-3 с медалями
- **Таймер** — обратный отсчёт до окончания конкурса
- **Статистика** — количество участников и голосов
- **Адаптивный дизайн** — работает на телефонах

### 🎨 Дизайн
- Яркий градиентный фон
- Карточки с фото котов
- Анимации при наведении
- Модальные окна подтверждения

---

## 🔧 Настройка Firebase (для полноценной работы)

### Шаг 1: Создать проект Firebase
1. Перейдите на https://console.firebase.google.com/
2. Нажмите «Создать проект»
3. Введите название (например: `best-cat-contest`)
4. Отключите Google Analytics (не нужен)
5. Нажмите «Создать проект»

### Шаг 2: Добавить веб-приложение
1. Нажмите значок `</>` (Web)
2. Введите название приложения
3. Нажмите «Зарегистрировать приложение»
4. **Скопируйте конфигурацию** (объект `firebaseConfig`)

### Шаг 3: Создать базу данных
1. В меню слева выберите «Realtime Database»
2. Нажмите «Создать базу данных»
3. Выберите режим «Начать в тестовом режиме»
4. Нажмите «Включить»

### Шаг 4: Создать Storage (для фото)
1. В меню выберите «Storage»
2. Нажмите «Начать»
3. Выберите режим «Начать в тестовом режиме»
4. Выберите регион (рекомендуется `europe-west`)
5. Нажмите «Готово»

### Шаг 5: Вставить конфигурацию
В файле `index.html` замените:
```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    databaseURL: "https://YOUR_PROJECT_ID-default-rtdb.firebaseio.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

на свои данные из шага 2.

### Шаг 6: Раскомментировать код
Найдите и раскомментируйте:
```javascript
// firebase.initializeApp(firebaseConfig);
// const database = firebase.database();
// const storage = firebase.storage();
```

Удалите или закомментируйте код "DEMO MODE".

---

## 📤 Деплой на GitHub Pages

```bash
# Создать репозиторий
git init
git add index.html
git commit -m "Initial commit"

# Подключить к GitHub
git remote add origin https://github.com/YOUR_USERNAME/best-cat-contest.git
git push -u origin master

# Включить GitHub Pages
# Settings -> Pages -> Source -> master branch
```

---

## 🔒 Правила безопасности (обязательно!)

### Защита от накрутки (рекомендуется)
Добавьте в Firebase Database Rules:
```json
{
  "rules": {
    "cats": {
      ".read": true,
      ".write": "auth != null || true", // или добавьте авторизацию
      ".validate": "newData.hasChildren(['name', 'owner', 'image'])"
    },
    "votes": {
      ".read": true,
      ".write": true,
      "$uid": {
        ".validate": "!data.exists()" // один голос на пользователя
      }
    }
  }
}
```

### Firebase Storage Rules
```json
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null 
                   && request.resource.size < 5 * 1024 * 1024 // 5 MB
                   && request.resource.contentType.matches('image/.*');
    }
  }
}
```

---

## 🚀 Расширенные функции (опционально)

### Добавить авторизацию
Можно добавить вход через Google/Facebook, чтобы:
- Один пользователь = один голос (гарантированно)
- Защита от накрутки
- Возможность редактировать свою заявку

### Модерация
Добавить статус "на модерации" для новых фото.

### Админ-панель
Отдельная страница для управления конкурсом.

---

## 💡 Советы по использованию

### Перед конкурсом
1. Протестируйте загрузку и голосование
2. Установите дату окончания в коде
3. Подготовьте призы для победителей

### Во время конкурса
1. Регулярно делайте бэкапы данных
2. Следите за накрутками
3. Публикуйте истории в соцсетях

### После конкурса
1. Объявите победителя
2. Вручите призы
3. Сохраните фото участников

---

## 📱 Тестирование

Откройте сайт на разных устройствах:
- 📱 Мобильный телефон
- 💻 Компьютер
- 📱 Планшет

Проверьте:
- Загрузку фото
- Голосование
- Отображение рейтинга

---

Удачи с конкурсом! 🐱🏆
